export enum EEndpoint {
    /**Endpoint for places*/
    places = 'places',
    /**Endpoint for place details*/
    placeDetails = 'place-details',
    /**Endpoint for constructing route by waypoints*/
    routing = 'routing'
}