export enum ERoutingParam {
    lang = 'lang',
    /**Points of route each waypoint separated by |, while latlon by ,*/
    waypoints = 'waypoints',
    /**Mode of driving*/
    mode = 'mode',
    units = 'units',
    traffic = 'traffic',
    details = 'details',
    avoid = 'avoid'
}